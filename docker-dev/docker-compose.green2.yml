version: '3.1'

services:

  api:
    image: gunyoung/dev:latest

    container_name: springboot-green-b

    environment:
      - "SPRING_PROFILES_ACTIVE=dev1"
      - LANG=ko_KR.UTF-8

    ports:
      - '8083:8081'